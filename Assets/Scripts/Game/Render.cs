using System;
using UnityEngine;

public class Render : MonoBehaviour
{
    internal void SetWorldState(Game.TerrainType[,] terrainMap, Building[,] buildingsMap, Unit[,] unitsMap)
    {
        throw new NotImplementedException();
    }
}
