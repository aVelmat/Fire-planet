using System;
using UnityEngine;
using static Game;

public class WorldGen
{
    internal static void GenerateWorld(TerrainType[,] terrainMap, Building[,] buildingsMap, WorldType defaultWorld, int seed)
    {
        throw new NotImplementedException();
    }

    public enum WorldType { 
        defaultWorld
    }
}
